{% extends 'base.html.twig' %}

{% block body %}

	<h1>Toute les catégories<h1>

			<section class="categories">
				{% for categorie in categories %}
					<article>
						<h2>{{ categorie.titre }}</h2>
						<div class="content">
							<img src="{{ categorie.image }}" alt="" width="180" height="130">
							<p>
								{{ categorie.contenu | raw }}
							</p>
							<a href="{{path('articles', {'id' : categorie.id} )}}" class="btn btn-info" role="button">Voir plus</a>
						</div>
					</article>
                    
				{% endfor %}
			</section>
		{% endblock %}
